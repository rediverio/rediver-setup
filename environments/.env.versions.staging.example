# =============================================================================
# Version Configuration - Staging
# =============================================================================
# This file contains version tags for Docker images.
# Copy to .env.versions.staging and update as needed.
#
# Usage: cp environments/.env.versions.staging.example .env.versions.staging
# =============================================================================

# API version (from exploopio/api)
API_VERSION=staging-latest

# UI version (from exploopio/ui)
UI_VERSION=staging-latest

# Admin UI version (from exploopio/admin-ui)
ADMIN_UI_VERSION=staging-latest

# Migrations version (from exploopio/migrations)
# Should match API_VERSION to ensure schema compatibility
MIGRATIONS_VERSION=staging-latest

# Seed version (from exploopio/seed)
# Should match API_VERSION to ensure data compatibility
SEED_VERSION=staging-latest
